# tmux 使用方法まとめ

## 1. インストール
環境に合わせて以下のコマンドを実行します。

- **Ubuntu / Debian**: `sudo apt install tmux`
- **CentOS / RHEL**: `sudo yum install tmux`
- **macOS**: `brew install tmux`

---

## 2. セッションの基本操作
セッションは、接続が切れてもバックグラウンドで残り続ける作業単位です。

- **新規起動 (自動命名)**: `tmux`
- **名前を付けて起動**: `tmux new -s [セッション名]`
- **デタッチ (作業を維持して離脱)**: `Ctrl + b` → `d`
- **セッション一覧を表示**: `tmux ls`
- **アタッチ (再接続)**: `tmux a -t [セッション名]`
- **セッションを終了**: `exit` または `Ctrl + d`

---

## 3. ウィンドウ操作 (タブのような機能)
一つのセッション内で画面全体を切り替えて使います。

- **新規作成**: `Ctrl + b` → `c` (create)
- **次のウィンドウへ**: `Ctrl + b` → `n` (next)
- **前のウィンドウへ**: `Ctrl + b` → `p` (previous)
- **番号を指定して移動**: `Ctrl + b` → `[0-9]`
- **ウィンドウの一覧から選択**: `Ctrl + b` → `w`

---

## 4. ペイン操作 (画面分割)
一つのウィンドウを分割して、複数のターミナルを同時に表示します。

- **左右に分割**: `Ctrl + b` → `%`
- **上下に分割**: `Ctrl + b` → `"`
- **ペイン間の移動**: `Ctrl + b` → `矢印キー`
- **現在のペインを全画面化 / 戻す**: `Ctrl + b` → `z` (zoom)
- **現在のペインを閉じる**: `Ctrl + d`

---

## 5. おすすめのカスタマイズ
`~/.tmux.conf` というファイルを作成し、以下の設定を追記すると便利です。

```bash
# マウス操作を有効にする (スクロールやクリックでのペイン選択が可能に)
set -g mouse on

# 設定ファイルの再読み込みコマンド
# tmux source ~/.tmux.conf